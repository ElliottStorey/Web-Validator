const isXss = (input) => {
    return 'Hello World';
};

const isEmail = (input) => {
    return `Your name is ${name}`;
};

const isPassword = (input) => {
    
};

module.exports = {
    magicString,
    displayMyName
};
